/* import rateLimit from "express-rate-limit";

const rateLimiter = rateLimit({
    windowMs: 1 * 60 * 1000, // 1 min represented as milliseconds
    max: 20,
    message: 'You have exceeded the rate limit of 20 reqs per minute!',
    headers: true,
});

module.exports = rateLimiter;
 */